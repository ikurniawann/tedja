import { UsersListPage } from "@/features/users";

// Direktori karyawan (menu Karyawan) — manajemen akun login pindah ke
// /dashboard/settings/users (menu Manajemen User).
export default function EmployeesPage() {
  return <UsersListPage />;
}
