import { RestaurantPage } from "@/features/pos/restaurant";

/** Tablet/kiosk entry — AppSidebar hides chrome when pathname is this route. */
export default function PosRestaurantTabletPage() {
  return <RestaurantPage />;
}
